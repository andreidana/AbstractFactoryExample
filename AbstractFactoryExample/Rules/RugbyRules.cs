namespace AbstractFactoryExample.Rules
{
    public class RugbyRules: IRules
    {
        public string GetEliminatePlayerRule()
        {
            return "No clue how it is done, but sure it is interesting";
        }
    }
}